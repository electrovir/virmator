{
    "license": "MIT",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "virmator frontend build",
        "compile": "rm -rf dist && virmator compile",
        "docs:check": "virmator code-in-markdown check",
        "docs:update": "virmator code-in-markdown",
        "format": "virmator format",
        "prepublishOnly": "npm run compile && npm run test:all",
        "preview": "virmator frontend preview",
        "spellcheck": "virmator spellcheck",
        "start": "npm install && virmator frontend",
        "test": "virmator test",
        "test:all": "npm run test:types && npm test && npm run spellcheck && virmator format check && npm run docs:check",
        "test:types": "tsc --noEmit",
        "test:web": "virmator test-web"
    }
}
