import {styles} from 'ansi-colors';

export const Color = {
    Info: styles.blue.open,
    Faint: styles.gray.open,
    Fail: styles.red.open,
    Warn: styles.yellow.open,
    Success: styles.green.open,
    Reset: styles.reset.open,
    Bold: styles.bold.open,
};
