import {defineElement, html} from 'element-vir';

export const VirApp = defineElement()({
    tagName: 'vir-app',
    render() {
        return html`
            Vir App goes here!
        `;
    },
});
